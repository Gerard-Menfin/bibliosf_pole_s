<nav class="navbar navbar-expand-lg bg-dark navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_home') }}">BiblioSF</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
            data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
            aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                {# EXERCICE : le lien déconnexion ne doit s'afficher que si on est connecté. Les liens
                                connexion et inscription ne doivent s'afficher que si on est déconnecté #}
                {% if app.user %}

                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_espace_lecteur') }}">
                            <i class="bi-book"></i> {{ app.user.userIdentifier }}
                        </a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" aria-current="page" href="{{ path('app_logout') }}">
                            <i class="bi-box-arrow-right"></i>
                        </a>
                    </li>

                {% else %}

                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_login') }}"><i class="bi-box-arrow-in-left"></i></a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_register') }}"><i class="bi-sign-intersection"></i></a>
                    </li>

                {% endif %}


                {# is_granted est une fonction qui renvoie true si l'utilisateur connecté à le rôle passé en argument. #}
                {% if is_granted("ROLE_ADMIN") %}

                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown"
                        aria-expanded="false">
                        Auteurs
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="{{ path('app_admin_auteur_index') }}">Liste</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_admin_auteur_new')   }}">Ajouter</a></li>
                    </ul>
                </li>

                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown"
                        aria-expanded="false">
                        Livres
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="{{ path('app_admin_livre_index') }}">Liste</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_admin_livre_new')   }}">Ajouter</a></li>
                    </ul>
                </li>

                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown"
                        aria-expanded="false">
                        Genres
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="{{ path('app_admin_genre_index') }}">Liste</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_admin_genre_new')   }}">Ajouter</a></li>
                    </ul>
                </li>

                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown"
                        aria-expanded="false">
                        Abonnés
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="{{ path('app_admin_abonne_index') }}">Liste</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_admin_abonne_new')   }}">Ajouter</a></li>
                    </ul>
                </li>

                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown"
                        aria-expanded="false">
                        Emprunts
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="{{ path('app_admin_emprunt_index') }}">Liste</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_admin_emprunt_new')   }}">Ajouter</a></li>
                    </ul>
                </li>

                {% endif %}

                <li class="nav-item">
                    <a class="nav-link">
                        <i class="bi-cart"></i> <span id="nbReservations">0</span>
                    </a>
                </li>
            </ul>
            <form class="d-flex" role="search" action="{{ path('app_recherche') }}">
                <input class="form-control me-2" name="search" type="search" placeholder="Search" aria-label="Search">
                <button class="btn btn-outline-success" type="submit"><i class="bi-search"></i></button>
            </form>
        </div>
    </div>
</nav>

